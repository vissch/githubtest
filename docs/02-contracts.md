# Frozen contracts (Phase 0)

> Part of the Trench Warfare 3D roadmap. See [README](../README.md) for the index.

These three contracts are frozen at the end of Phase 0. Changing any of them requires a version bump of the
replay file format and a note in this file. Every track builds against them independently.

### 3.1 Command contract
```
struct SimCommand { uint tick; byte player; CommandType type; int a; int b; float3 pos; }
enum CommandType {
  DeployUnit(slot=a),                  // spend silver, queue at spawn point
  TrenchAdvance(trenchId=a),           // ">>"
  TrenchSelectAdvance(trenchId=a, unitMask=b), // roster "↑" partial advance
  TrenchLock(trenchId=a, on=b),        // lock icon
  TrenchFallback(trenchId=a),          // "↩"
  UnitStance(unitSlot=a, stance=b),    // 3D addition: prone/crouch/sprint override
  SupportFire(abilityId=a, pos, b=AbilityArgs), // HE, gas, smoke, creeping barrage, strafe; b packs heading, pattern, length (0 = plain)
  UnitAbility(unitSlot=a, abilityId=b, pos), // officer smoke call, bangalore, etc.
  SetRally(pos),                       // logistics: where reinforcements collect
  Surrender
}
SimWorld.Step(NativeSlice<SimCommand> cmdsForThisTick)
```
Commands are validated in-sim (cost, cooldown, ownership); invalid ones are dropped deterministically.

### 3.2 Map data contract
`MapData` blob: dims, cell sizes, heightfield, nav layers/cost, trench definitions (`TrenchDef`: id, side,
ordered traverse cells, fire-step cells, links), spawn points, supply road polyline, sector objectives
(`ObjectiveDef`: id, type OutpostLine/MainLine/ReserveLine/HQ, cells, owner), wire belts, mud polygons,
static emplacements, scripted trigger volumes.

### 3.3 Pose + Event stream contract
```
struct UnitPose { float3 pos; half yaw; ushort animRow; half animT; byte archetype; byte flags; byte lod; byte team; }
enum SimEventType { Shot, Hit, NearMiss, Explosion, CraterStamp, Death, StanceChanged, Suppressed, Pinned,
  UnitSpawned, UnitEnteredTrench, UnitLeftTrench, TrenchCaptured, ObjectiveCaptured, GasCloudSpawned,
  SmokeSpawned, VehicleTrackHit, VehicleStalled, VehicleDestroyed, WireBreached, AbilityFired, WaveStarted,
  MissionTriggerFired, CellBurning, MatchEnded, CommandRejected, PropChanged,
  VehicleFired, VehicleArmourHit, VehicleModuleHit, VehicleOnFire, VehicleCrewLost, VehicleBailedOut,
  VehicleKnockedOut, VehicleCookOff, VehicleBogged, VehicleDitched, VehicleCrushed, VehicleRepaired,
  VehicleLegLost, VehicleClawed, CraftInbound, CraftBeached, ShipFired, OrderFoundNoOne, UnitAlight }
enum DeathCause { Blast = -1, Gas = -2, Crush = -3 (reserved), Burning = -4, Beam = -5 }   // Death.b when no slot did it
struct SimEvent { uint tick; SimEventType type; int a; int b; float3 pos; float3 dir; float scalar; }
```
Presentation consumes the ring buffer once per frame; audio, VFX, UI, ragdolls and mission dialogue all hang
off it. Mission scripting runs **inside** sim (deterministic) and emits `MissionTriggerFired`.

## Command validation rules (sim side)

| Rule | Behaviour on violation |
|---|---|
| `player` must own the trench / unit slot referenced | command dropped, `CommandRejected` debug counter incremented |
| `DeployUnit` requires silver ≥ cost and slot unlocked for this mission | dropped |
| `SupportFire` / `UnitAbility` require cooldown expired and ability unlocked | dropped |
| `pos` must lie inside the map bounds | clamped to bounds |
| Commands for a tick are sorted by `(player, arrivalIndex)` before execution | guarantees identical order on every peer |

Dropping is deterministic because validation reads only sim state, never presentation state.

## Event semantics

- Events are append-only within a tick and cleared at the start of the next `Step()`.
- `a` / `b` carry slot indices, trench ids, objective ids or ability ids depending on `type` (see the enum comments in `SimEvents.cs`).
- `scalar` carries damage, concentration or radius; `dir` carries shot direction or wind.
- `AbilityFired`: `pos` is the target, or where a line starts; `dir` is the heading times the length for a line
  ability or pattern (zero for a point); `scalar` is an area's radius or a line's corridor half width. `SmokeSpawned`
  and `GasCloudSpawned` come once per source (`a` = ability, `b` = player, `dir` = the line's heading, `scalar` =
  concentration).
- `Death`: `b` is the killer's slot when one did it (a shot, a claw, a track) and a `DeathCause` below zero when a
  blast, gas or fire did; a blast's dead carry the way they were thrown in `dir.xz` with `dir.y = 1`, and how hard in
  `scalar` (m/s). The same-tick `Explosion` says which weapon. Consumers test `b < 0`, never `b == -1`.
- Presentation must tolerate dropped events (ring buffer overrun is reported via `SimEvents.Overrun`) and rebuild from state, never rely on events for correctness.

## Change log

| Date | Change | Replay format |
|---|---|---|
| 2026-09-20 | Replay header gains `FormatVersion` (ushort), `MapHash` (ulong, `MapData.Hash()`), `DataHash` (ulong, baked stat tables; 0 until C2 wires it). `ReplayPlayer.Parse` rejects a mismatched format version. | v1 → **v2** |
| 2026-09-20 (M1) | Map data: `TrenchDef.WidthMeters` (tracked vehicles cross a trench ≤ 3.5 m), `MapData.CellTrenchId` (nav cell → trench id, derived), `MapData.Version` (bumped on every nav/height mutation; the tick hash covers it instead of the arrays). Per-slot state: `SimWorld.SourceTrench` (trench a unit last left under orders; the `↩` target), hashed. Layout unchanged, but hashes of replays recorded before this commit no longer verify. | v2 (hash content) |
| 2026-09-20 (playtest) | Deploy spawn jitter now draws from one RNG stream per deploy (`player + 16 * n`, n = that player's deploys this tick) instead of one per (tick, player); same-tick deploys no longer share a spawn point. `SeparationJob` gives coincident pairs antisymmetric normals and caps the push at 6 m/s. Layout unchanged; replays recorded before this commit no longer verify. | v2 (hash content) |
| 2026-09-20 (M1.5 combat) | Per-slot state: `SimWorld.FireCooldown` (ticks until the primary weapon fires again), hashed. `SectorControlSystem.States` (objective owner / capture progress) joins the hash. Deploy spawn jitter widened from ±6 m to ±30 m in X. `MatchSim(config, map, combat)` can leave target acquisition and direct fire out for movement-only runs. Layout unchanged; replays recorded before this commit no longer verify. | v2 (hash content) |
| 2026-09-20 (M1.5 support) | `SupportFire` is now consumed (`a` = `OffMapAbilityId`, `pos` = target; HE barrage and chlorine implemented). New hashed state: `OffMapAbilitySystem.Cooldown` and `.Scheduled`, `GasSmokeSystem.Gas` and `.Sources` (only while a cloud exists), `DeformationSystem` queue, count and checksum of applied `CraterStamp`s. Craters mutate `MapData` height and nav layers at run time; `MapData.Version` carries that into the hash. `GasSmokeSystem` uses one `float` grid instead of two `half` grids. Layout unchanged; replays recorded before this commit no longer verify. | v2 (hash content) |
| 2026-09-20 (M1.5 playtest map) | New generated map `GreyboxMapGenerator.CreatePlaytest` (MapId 2: 300 x 480 m, reserve and front trench per side, six objectives). Freshly deployed infantry now walk to the rear-most trench their team owns (`FlowFieldManager.RearTrench`) instead of the front trench; a locked trench passes them up the chain. Infantry within 8 m close-assault vehicles (`CombatTables.CloseAssault*`). Behaviour change only; replays recorded before this commit no longer verify. | v2 (hash content) |
| 2026-09-21 (A4 battlefield) | Map data gains `WaterLevel`, `Props` and the derived `CellCover` grid (all in `MapData.Hash`); `DeformationSystem` now also breaks props, leaves wrecks (`VehicleDestroyed` is emitted by `SimWorld.Despawn`) and breaches wire; new event `PropChanged`; `DirectFire` adds prop cover. New generated map `BattlefieldGenerator.Create(BattlefieldParams)` (MapId 3). The replay header carries `MapParams` (the serialized params a generated map is rebuilt from). | **v3** (header layout) |
| 2026-09-21 (battlefield layout) | `BattlefieldGenerator` output changed for the same params: the default field is 90 x 240 m with a layout that scales with the params, and trenches are bent (20 m fire bays up to 4 m forward or back, joined by traverses; `GreyboxMapGenerator.AddFireTrench` / `AddLineObjective` take per-column `offsets`, objectives follow the bays). Layout unchanged; replays of generated battlefields recorded before this commit no longer verify. | v3 (map content) |
| 2026-09-21 (organic placement) | `PropDef` gains `Scale` (drawn size of a clump member; hashed). `BattlefieldGenerator` output changed again for the same params: trench lines wander on noise with jogs and unevenly spaced ladders (`AddFireTrench` takes `ladders`), the dug-in ground and the wire follow them, the river meanders with a varying width and ragged banks, craters fall in salvos (one big, a few medium, more small), and the wood stands in Poisson-spaced clumps with a big / medium / small hierarchy. Replays of generated battlefields recorded before this commit no longer verify. | v3 (map content, prop layout) |
| 2026-09-22 (A5b armour) | Tanks. Roster slot 4 is the Maw for player 0 and the Tusk for player 1 (`VehicleArchetype.Maw = 4`, `.Tusk = 5`, `RosterEntry.Maw` / `.Tusk`; a vehicle's Hp is its hull structure). New systems: `TankGunnerySystem` (order 705, combat only: the main guns; per slot × 2 gun yaw, reload, target and gun health, per slot crew factor, all hashed; HE goes through `BlastSystem`, AP hits through `PendingHits`) and `VehicleModulesSystem` (order 730, the A5 stub implemented: armour via `Armor.Penetrates` / `FacingOf` / `PenetratesTop`, modules, crew, fire, bail-out, knock-out, cook-off, repairs; per-slot state hashed plus a checksum). `VehicleKinematicsSystem` gains hashed per-slot state (`Gen`, `SpeedFactor`, `HaltTicks`, `CrossTrench`, `DitchTicks`, `BogTicks`) and a profile per archetype (bridging and ditching in trenches, bogging, slopes, pivot turns, tank separation); it crushes wire (`WireBreached`), fells trees under a hull (`PropChanged`) and runs men down. `BlastSystem` no longer damages vehicles (the armour model does); a close assault is a `VehicleHit` on the armour. New `UnitFlags.KnockedOut`; `VehicleModule` gains values (the first five keep their numbers); `SimEventType` gains `VehicleFired` … `VehicleRepaired` (appended), with `VehicleKillCause`; `VehicleTrackHit.b` = side, `VehicleStalled.b` = stalled / restarted. Replays recorded before this commit no longer verify. | v3 (hash content) |
| 2026-09-22 (A3 garrison posts) | A man garrisoning a trench is given a POST in it: `SimWorld.PostCell` (nav cell, -1 none) and `SimWorld.PostKind` (0 none, 1 firing at the parapet, 2 reserve), both hashed, both cleared on spawn and despawn. `TrenchGarrisonSystem` (order 820, the A3 stub in `Sim/Units/TrenchGarrison.cs` filled in, not a new system) lays the posts out once from `TrenchDef.FireStepCells` and `TrenchCells`, skipping ladder cells and thinning the rest by a hash so they are irregular, then hands them out in ascending slot order, about three men in five at the parapet. `MovementSystem` walks a garrisoned man to his post and holds him there, lets him cross a ladder on the way (he still may not rest on one), puts only a man at a firing post up on the fire step once he has arrived, and turns him to the trench's `FacingYaw` rather than to his target's bearing. `StanceSystem` stays a stub: stance is decided in the movement job, which has the movement context. Replays recorded before this commit no longer verify. | v3 (hash content) |
| 2026-09-22 (A3 landings, B2 coast) | The generated battlefield can be a COAST. `BattlefieldParams.Sea` (serialized, appended, so an older blob still reads) adds `BattlefieldGenerator.SeaMargin` = 36 m of sand and shallows BEYOND the layout, so the default field is 90 x 276 m while every trench, objective, spawn and wire belt stays exactly where it was. `MapData` gains `SeaSide` (255 inland), `SeaStartZ`, `ShoreZ` and `SeaLevel`, all four in `MapData.Hash`, with `HasSea`, `SeaTeam`, `SeaAway` and `Offshore(z)` derived; `ToWorldInit` takes the goal line from the end of the LAYOUT rather than the end of the map, or team 0 would advance into the sea. New `SeaLandingSystem` (order 320, `Sim/Match/SeaLanding.cs`) with `ISeaLift`: `SimWorld.Deploy` offers every paid-for unit to `SimWorld.SeaLift` first, and on a coast the team whose rear it is goes aboard a craft standing 96 m off instead of appearing at its spawn point. A craft runs in, grounds short of the waterline, drops its ramp and puts its men on the sand a few ticks apart; a full lift refuses and that unit spawns as before, so the boats can never block a player. Six hulls, eight berths, a tank taking a hold to itself; all craft state is hashed. An empty craft comes in with stores every 55 s so the shore is never still. `SimEventType` gains `CraftInbound` and `CraftBeached` (appended). Replays recorded before this commit no longer verify. | v3 (map content, hash content) |
| 2026-09-22 (A5b walkers) | The owner's four crab machines (`Tools/crabsplit.py` → `Resources/Vehicles/Pincer|Kettle|Censer|Pavise`). **`RosterEntry.SlotCount` 5 → 8**: each side keeps its tank in slot 4 and gains three walkers, player 0 the Pincer, Pavise and Banner, player 1 the Kettle, Censer and Redoubt (`SlotCooldown` / `SlotUnlocked` are sized by it and hashed, so every replay's per-slot state changes length). New archetypes `VehicleArchetype.Pincer = 6, Kettle = 7, Censer = 8, Pavise = 9, Banner = 10, Redoubt = 11` with `IsWalker` and `IsArmoured`; `TankGunnerySystem` and `VehicleModulesSystem` now run for anything armoured rather than for tanks only. `VehicleProfile` gains `Walker` and `Legs`: a walker steps over a trench up to its `TrenchCrossWidth` at `StepOverSpeed` instead of bridging it, never ditches (`DitchChance` 0), crosses wire without slowing OR breaching it, and is barely slowed by mud or shell holes. `TankGun` gains `Indirect` and `RangeMin` (the Kettle's mortar fires without line of sight, prefers men below a parapet, and will not engage inside 46 m); `TankSpec` gains `Unmanned` (no bail-out: nobody is inside a walker), `ClawReach`/`ClawDamage`/`ClawSeconds` (a walker crushes what comes within reach of its front — a man dies, a hull takes a close assault at `ClawPenMm`) `GasEverySeconds`/`GasStrength` (the Censer lays chlorine from the drum on its back until the drum is holed) and `StandardRadius`/`StandardSteady` (the Banner's standard: friendly infantry within 26 m lose suppression 9/s faster, swept every 5 ticks). The Redoubt carries no gun at all (`GunCount` 0) and the heaviest claws and plate on the field. New hashed state: `TankGunnerySystem.ClawCooldown` and `VehicleModulesSystem.LegsLost` (a bit per leg, left side first) — a hit that would break a track takes one leg off a walker instead, each leg costs it 16 % of its speed, and a side fails only when its last leg has gone. New events `VehicleLegLost` and `VehicleClawed`. Replays recorded before this commit no longer verify. | v3 (hash content, roster length) |
| 2026-09-26 (A5 fire, docs/21) | **The Death event says what killed a man and how hard.** `Death.b` is a `DeathCause` (`Blast` -1, `Gas` -2, `Burning` -4, `Beam` -5; `Crush` -3 reserved, tracks and claws keep the vehicle's slot) when no slot did it; a blast's dead carry their knock (`dir.y = 1`, `dir.xz` the way, `scalar` m/s) instead of a constant (0, 1, 0), computed in `BlastJob` for the dead the way it is for a survivor. `SimWorld.Despawn` gains a `knock` parameter. `BurningSystem` (order 725, `Sim/Combat/Burning.cs`, the A5 stub implemented and registered) sets men and ground alight: `AlightUntil` per slot and `Cells` (nav cell, tick it stops, player), both hashed; a man alight loses `BurnDps` 12 a second, is suppressed, leaves a trench he garrisons and runs for his HQ like a gassed man, catches from burning ground, and dies of `Burning`; `UnitFlags.Burning` is now set for infantry (VehicleModules keeps setting it for vehicles). `Impact` gains `BlastShape.Incendiary` = 3: it bursts like a shell and then lights every man and nav cell inside its radius (`BurstManSeconds` 7, `BurstCellSeconds` 5, at most 96 cells). New event `UnitAlight` (a = slot, b = 1 caught / 0 out; a dead man's fire goes out with one b = 0 too, so a b = 0 may name a slot with no live man; scalar = seconds), appended. `SimRandom.SystemId` reserves `Abilities` 13, `Mines` 14, `Beam` 15, `Burning` 16. Replays recorded before this commit no longer verify. | v4 → **v5** (hash content) |
| 2026-09-26 (A5 abilities, docs/21 phase 5) | **Line abilities.** `SupportFire.b` is `AbilityArgs.Pack(heading, pattern, length)` (`Sim/Core/AbilityArgs.cs`: 9 + 4 + 8 bits; 0 = the plain ability at its own length, which is what every earlier command carried). New ids `StrafeRun` = 10 (32 bursts of 60 in 5 m along an 80 m corridor), `Beam` = 11 (reserved for SIM-C). HE barrage offers patterns disc / line / box; chlorine offers point / creeping (8 sources of 30 walking 8 m every 3 s); `CreepingBarrage` (10 lifts of 4, 6 m every 4 s) and `SmokeScreen` (5 sources of 30 on a 40 m line, 30 s) are implemented. `ScheduledPayload` gains `Dir`, `Radius`, `Kind` (`PayloadKind`) and `Ticks` (all hashed); `AbilitySlots` is 16; `AbilityStats` gains `HalfWidth`, `ShellSuppression`, `CraterRadius`, `Steps`, `StepTicks`, `StepMetres`, `SafeBehind`, `Patterns`. `Impact.SafeBehind`: the firing player's own men within that many metres behind a directional burst are not touched (`BlastJob` reads `Team`). `GasSmokeSystem` gains the smoke field (`Smoke`, `SmokeSources`, `SmokeActive`, hashed only while up, diffusion 0.06 / decay 0.006) and `MetresThrough`; `Sim/Combat/SmokeLos.cs` counts the metres of thick cloud (> 10) on a line in 2 m steps; `TargetAcquisition` loses a target past `SmokeBlindMetres` 12.5 of it, `DirectFire` takes 8 % accuracy per metre (floor 0.2) and halves the suppression of a man inside it. The HE scatter now draws from `SimRandom.SystemId.Abilities`. `AbilityFired.dir/scalar`, `SmokeSpawned` and `GasCloudSpawned` fields as in Event semantics. Layout unchanged; replays recorded before this commit no longer verify. | v5 → **v6** (hash content) |
| 2026-09-26 (A5 mines, docs/21 SIM-D) | **Mines and tripwires.** `Sim/Combat/Mines.cs` (`MineSystem`, order 1130, after VehicleKinematics: it reads the tick's positions and its bursts resolve in the next tick's Blast) holds `Mines` (hashed after `Beams`: pos, dir, length, arm tick, player, kind, state). `Place` is a system call today (the sapper's `UnitAbility` will call it once units-meta lands); a mine arms in 20 ticks. An enemy man within 1 m of a mine or 0.6 m of a tripwire's line, or an enemy hull within its half width, sets it off (slot order, the first man): `MineTriggered` (a = index, b = victim, pos, dir = the victim's heading, scalar = kind) and an `Impact` of `BlastShape.Mine` = 5 (a mine: 260 dmg, r 4, +50, crater 1.5; a tripwire: 90 dmg, r 4.5, +40, no crater; `Source` = 70 + kind) that men take as any burst and a hull takes on the nearer track (`VehicleModules`: `MineTrackDamage` 0.6, 15 % of the damage off the structure). The layer's own side never sets one off. A burst that digs a crater cooks off the mines within 1.5 x its crater radius: `MineCleared` (a = index, b = the burst's player) now, their own bursts 4-20 ticks later. `MinePlaced` (a = index, b = player, pos, dir = heading x length, scalar = kind). A spent mine keeps its index; `Place` reuses spent slots; at most 256. Events appended. Replays recorded before this commit no longer verify. | v7 → **v8** (hash content) |
| 2026-09-26 (A5 beam, docs/21 phase 5) | **The beam.** `OffMapAbilityId.Beam` = 11 is implemented: cost 300, cooldown 3600, warm-up 80, a `PayloadKind.BeamStart` whose `Dir` is heading x length and `Ticks` the sweep (120). `Sim/Combat/BeamSystem.cs` (order 723, between Blast and Burning) holds the running sweeps (`Beams`, hashed): the head walks the corridor, the 4 m behind it burn; a man within the half width (2 m) takes 300 a second (x0.7 in a trench), is suppressed, catches fire (`BurningSystem.Ignite`, `UnitAlight`) and dies of `DeathCause.Beam` (`dir` = the beam's heading, `scalar` = 4); a vehicle within the half width of its hull loses 1200 a second off the structure and emits `VehicleArmourHit` (b = -1, scalar 1) each tick; every 4 ticks the head queues an `Impact` of `BlastShape.Beam` = 4 (damage 60, radius 3, no crater) that `BlastJob` and `VehicleModules` skip and `Deformation` wears props with, whose `Explosion` (`dir.y` = 4) is the picture's scorch cue. `BeamSystem.MaxPerPlayer` (2) caps the sweeps in flight per player as a safety bound: the cooldown (3600) outlasts a sweep (200), so a player never has two and a second command is rejected by the cooldown first. Replays recorded before this commit no longer verify. | v6 → **v7** (hash content) |
| 2026-09-24 (A4/A5 destruction) | **The ground is compared state.** New `TerrainHashSystem` (`SimSystemOrder.TerrainHash` = 50) steps nothing and folds `MapData.Hash` into `SimWorld.Hash()`, closing the gap docs/03 recorded and deferred to "the next replay-format break": the heightfield, nav layers, cost, trench ids and cover are compared every hashed tick. `MapData` gains `Holes` (`HoleRecord` per shell hole, so a second shell WIDENS one instead of only deepening it: `R' = sqrt(R^2 + 0.6 r^2)`, capped at 12 m, spoil bounded per hole at 0.5 m), `CellTrenchDist` (decimetres per height cell to the nearest trench or ladder, built once by `BuildTrenchDistance`) and `Bedrock` — all hashed. `CraterStamp` gains `Kind` (`CraterKind.Bowl` / `Mound`: a mound RAISES the ground for a building's rubble) and `ApplyDynamic`, which merges into an existing hole, throws up a rim, clamps depth to the bedrock and the water table, and scales every carve by the trench guard band — nothing within 1 m of a trench moves and the next 4 m move progressively more, so **a shell never caves a trench in or undercuts one** (owner: the trench always stands). `Impact` gains `Dir` (the shell's flight, XZ; zero = none), `Shape` (`BlastShape.Shell` / `Masonry` / `CookOff`) and `Rubble` (metres of mound a collapse heaps up); the `Explosion` event now carries the direction in `Dir.xz` and the shape in `Dir.y`. `BlastJob` cuts a burst by `BlastRules`: trench bay **0.7** (was 1.0 — a trench always gives limited protection), traverse 0.5, outside 0.35, a shell inside a trench reaching the open field 0.45, terrain shadowing 0.55 (`HeightfieldRaycast`, bounded to 48 rays per impact in slot order), and ±0.3 along the flight direction; floor 0.08. Suppression is leaned but deliberately NOT shaded. `VehicleModulesSystem`: a tank's track and engine now fail by DEGREES like a walker's legs (`TrackHealthFactor`, `TrackFactor`, `EngineFactor`; `Immobilised` below 0.2 rather than at 0, `VehicleTrackHit` and `VehicleStalled` fire on crossing those thresholds). Replays recorded before this commit no longer verify. | v3 → **v4** (header layout, hash content) |
