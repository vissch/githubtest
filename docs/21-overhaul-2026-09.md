# The six-phase overhaul (2026-09-26)

> Part of the Trench Warfare 3D roadmap. See [README](README.md) for the index. Plan of record for the owner's
> overhaul directive of 2026-09-26; the full working plan lives on the workstation
> (`~/.claude/plans/pasted-content-id-f06f-system-directive-dazzling-clover.md`) and this page carries its substance.
> Work happens in worktree `githubtest-overhaul` (`lane/show/overhaul`, SIM seams on `lane/sim/overhaul`).

## What was asked, and what already exists

| Phase | Directive | Already there | To build |
|---|---|---|---|
| 1 Scale | Soldier = 1.0; strict bounds for man-made objects, free variance for organic ones; audit every asset | `FigureMetrics` (2.0 m man), `VehicleSize` baked into meshes, per-kind looks in `PropLayout` (learned when men were 2.67 m, never rescaled: a FieldGun is 15 m) | a class + bounds table, an audit test and report, clamped looks, corrected look values |
| 2 Scatter | Grass in patches, denser near vertical things, none on trodden paths, flowers only in grass, camp kit only in trenches and rear zones | seeded composer steps, `Gather` clumps, `Open()` rejects trench cells, `CellTrenchDist` | traffic/vertical/patch fields, layered rules, unnamed grass modules, per-instance tint, trench-interior layer |
| 3 Trench | Modular lining that shatters under heavy fire, brief effect, pooled fragments | 2 m instanced panels that already break into ring-buffer debris, MG wear | damaged intermediate state, heavy-ordnance class, per-band debris caps, `FrameBudget` coverage |
| 4 Deaths | Density-scaled launches and pile-ups, flame death, wider death pool | 12 death clips, blast throw arc, gibs, 600-body buffer, `Burning` clip, presentation flamethrower | Death event cause/strength, `BurningSystem`, density ring, tumble, pile-up, char shader, smoulder, six clips + bake |
| 5 Abilities | Line strafe, beam, mines by sappers, HE/smoke/creeping-gas patterns | HE disc and chlorine point; 9 ids declared; drag code in selection | packed line arguments, strafe, beam, patterns, smoke grid + LOS, sapper archetype + mines, targeting UI, effects |
| 6 Meta | Home Front diorama with faction upgrades; continental map with country nodes; staging screen | screen stack, Armoury tiles, units-meta's factions/loadouts (replay v6) | campaign asset, profile save, upgrades block in `SimConfig`, two scenes + views, three screens, debrief rewards |

## Owner decisions (2026-09-26, `reference/decisions.md`)
Both lanes in one session (seams first, alone); man-made props true to the soldier while machines stay giant;
death physics are VAT launched bodies (no ragdolls); mines are laid by a sapper unit during the match (no
preparation phase). Assumed by default: the beam is an off-map line-targeted sweep; Iron and Brass are the playable
factions; explosions batches B/C and the destruction-v2 LOD/scar batches stay parked.

## Rules this work runs under
- Seams (`SimWorld` fields and `Hash()`, `SimConfig`, command meanings, the events contract, archetype ids, asmdef
  lists, `FormatVersion`) land as their own commit, alone, first. `Hash()` appends at the end. Replay versions are
  assigned at landing time in landing order (units-meta v5/v6 first if it lands first; then upgrades, deaths,
  abilities, beam, sapper). Each seam: a `02-contracts.md` row, a `DeterminismReplayTests` case, hash coverage.
- No editor opens under ~4 GB free (`reference/workflow.md` section 2). The loop is the offline Roslyn compile, then
  the batch gate with the editor closed. Bakes, scene builds and Play checks wait for an editor session.
- New code goes in new files or partials (`CombatFx`, `AnimationController`, `VATRenderer`, `BattlefieldComposer`
  are at or past the 1,000-line split point). Every new test, hook, tool and flag is routed in `reference/` or
  `validate.py` fails.

## Phase 4: deaths
**SIM-A seam.** `DeathCause { Blast = -1, Gas = -2, Crush = -3 (reserved), Burning = -4, Beam = -5 }` in the Death
event's `b` (killer slot when >= 0); `dir` = knock direction (blast: `dir.y = 1`), `scalar` = knock speed. `Blast`
computes the knock for the dead too. `BurningSystem` (order 725) with hashed `AlightUntil`, burning cells, 12 hp/s,
`UnitAlight` events, `Despawn(..., Burning)`.
**SHOW.** Six clips (`DeathBackHeadshot`, `DeathWalking2`, `DeathStagger`, `DeathGas`, `DeathCrushed`,
`DeathBurning`) and one bake. `AnimationController.Death.cs` (partial): death latch, a 256-record death ring read
by `CombatFx.Deaths.cs` (closes the slot-reuse hazard), density within 4 m / 12 ticks scaling the throw (`1 + 0.35 *
min(n, 5)`), tumble encoded in `VatInstance.Tint` (32 pitch steps), pile-up from a 2 m body-count grid in
`VATRenderer.Fallen.cs`, gibs scaled by density. Flame death: `VatPad` bits 22-23 = char level, `VAT_URP` blackens
and embers (no discard), a smoulder list re-emits smoke cards 8 s, charred bodies shrink as they sink.

## Phase 5: abilities
**SIM-B seam.** `AbilityArgs.Pack(heading, pattern, length)` in `SupportFire.B`; `ScheduledPayload` gains `Dir`,
`Radius`, `Kind`; `AbilitySlots` 16. StrafeRun (id 10): 24 light impacts along a corridor. HE patterns disc/line/box;
CreepingBarrage (id 2): 10 lifts of 4 shells stepping 6 m every 80 ticks, own men safe 15 m behind. SmokeScreen
(id 6): a second grid in `GasSmokeSystem`, `SmokeLos.MetresThrough` cuts sight at 12.5 m and accuracy 8 %/m.
Creeping gas: chlorine pattern 1, eight sources stepping 8 m.
**SIM-C seam.** Beam (id 11): `BeamSystem` (order 723), hashed active beams, a segment sweep at 240 dps (x0.7 in a
trench), 1,200 dps to hulls, lights men, scorches props through `BlastShape.Beam` impacts.
**SIM-D seam (after units-meta).** `InfantryArchetype.Sapper` in both faction pools with mine and wire charges;
`UnitAbilityId.LayMine/LayTripwire` consumed by `SpecialAbilitiesSystem`; `MineSystem` (order 1130): hashed mines
and tripwires, enemy-only triggers, HE clearing, track hits.
**SHOW.** `UI/Abilities/AbilityTargeting.cs` replaces the `TestPanel` arm path (click, drag-a-line, pattern
sub-buttons, `SceneHooks.AimPreview` drawn by `CombatFx.Aim.cs`); six support cards; `StrafeFx` (biplane flyover),
`BeamFx` + `TW/Beam (URP)`, `MineFx`, `CombatFx.Smoke.cs`; sapper LAY buttons on the selection panel.

| Id | Cost | CD (ticks) | Warm-up | Target | Payload |
|---|---|---|---|---|---|
| HeBarrage 1 | 150 | 1200 | 80 | area / line / box | 12 x (150, r 8) over 120 t |
| CreepingBarrage 2 | 250 | 2400 | 120 | line + heading 60 m | 10 lifts x 4 x (120, r 8) |
| ChlorineGas 3 | 120 | 1800 | 60 | point / creeping | conc 40 x 240 t; 8 x conc 30 stepping 8 m |
| SmokeScreen 6 | 60 | 600 | 40 | line 40 m | 5 sources conc 30, 600 t |
| StrafeRun 10 | 180 | 1800 | 100 | line + heading 80 m | 24 x (45, r 3) over 40 t |
| Beam 11 | 300 | 3600 | 80 | line + heading 60 m | 120 t sweep, 240 / 1200 dps |
| Sapper mine / tripwire | 1 charge | none | 3 s place | point / line <= 12 m | 260 r 4 / 90 r 4.5 |

## Phase 6: Home Front and the campaign map
`UI/Campaign/CampaignDefinition` (six country nodes, 3-4 ordered `MissionCard`s each, prerequisites, gold
25/40/75), `FactionBuildings` (a code table: building stages 40/80/150 gold cap the tiers they sell; unit tracks at
15/20/40/60/80/100; global lines for armour, silver, income, ability cooldown/cost, veteran survival),
`CampaignProfile` + `ProfileStore` (`profile.json`, versioned like settings). SIM seam after units-meta: `SimConfig.
UpgradesA/B` (+5 % per tier on hp, damage, accuracy keyed by faction pool index; armour +2 mm per tier) and
`AbilityMaskA/B`, applied at `Spawn`, in `DirectFire`, `TankGunnery`, `VehicleModules`, `OffMapAbilitySystem`;
hashed; in the replay header. New assembly `TW.Presentation.Meta` (`HomeFrontDiorama` on `HouseKit` chunk masks
for visible stages, chimney smoke, lamps; `StrategicMapView` on a generated continent mesh with pins, a front-line
ribbon and fog; `MetaCamera`), driven by `TW.UI` through `IHomeFrontView`/`IStrategicMapView` in
`Presentation/Core`. Scenes `HomeFront` and `StrategicMap` built by `ShellAssetsBuilder.BuildMenuScenes`; screens
HomeFront, StrategicMap, Staging (battalion slots from the faction pool, tiers, up to two ability picks; DEPLOY
writes loadout, upgrades and mask into the launch request), Debrief awards gold, Main Menu gains CAMPAIGN and HOME
FRONT.

## Phases 1-3: environment
**Phase 1.** One soldier unit (SU) = `FigureMetrics.HeightM` (1.78 m bake x `UnitScale`, 2.0 m); the readability
grow is a men-only exception. `Presentation/Terrain/AssetScaleTable.cs`: `ScaleClass { Strict, Structure, Organic,
Machine }` with bounds per module key (imported props by `Module.Name`, procedural kit by field name, buildings,
vehicles); `AssetScaleReport.Measure` generates the real field with no scene and applies looks exactly
(`PropLayout.Style` extracted from `BattlefieldProps.Styled`); `AssetScaleTests` fails a Strict/Structure anomaly;
`Editor/AssetScaleAudit.cs` writes `docs/reference/asset-scale.md` by menu or `-executeMethod`. Enforcement clamps
in `BattlefieldProps.Emit` and `Placement` (not `Styled`, which returns early without a look) and `Restyle` keeps
`Module.Size` consistent. `Tools/looks.py` patches the looks that were learned when men were 2.67 m: Sandbag 2.99
-> 1.20, FieldGun 3.50 x 2.83 x 5.58 -> 1.10 x 0.89 x 1.75, ArmouredStand -> 1.41, SodShelterRuin -> 1.30, MGNest
-> 0.85, SplitStump/FallenLog become multipliers so the generator's sizes return. Organic modules get wider ranges
and a per-instance tint; vehicles are audit rows only.
**Phase 2.** `BattlefieldComposer` is split into partials by step, then `Presentation/Terrain/Scatter/` adds fields
built once per map at 2 m (Traffic from trench/link cells, corridors between opposing links through wire gaps, the
supply road and the cart-rut band; Vertical from obstructions the composer records as it emits; Patch from seeded
noise; Open from nav layers, banks and structure footprints; winter and coast gates) and layers (grass on two
unnamed micro/small modules with page caps, flowers only inside grass, camp kit, crates, lanterns and wall debris
only on trench cells, dugout sites and the rear band). Per-instance tint via an `_INSTTINT` Toon variant kept by a
ShaderKeep material. The scatter step runs after sites, houses and landmarks; recomposition re-emits cached
placements filtered by fresh craters and `Suppress`. `ScatterRulesTests` on a hand-built field.
**Phase 3.** The 2 m lining panels already break; the gap is a Damaged state: damaged variants of walls, bags and
floors from `BuildTrenchVariants`, `TrenchSectionState` (damaged at 50 %, heavy ordnance = radius >= 6 or power >= 1
in the inner half of its reach shatters outright), shared identity through `Key(rule.Intact ?? module)`,
`BattlefieldProps.Swap`/`Replace`, a per-strike piece budget, `DebrisRenderer.ZoomShare` and a 12 s `Life` for
lining pieces, `FrameBudget` coverage of prop draws. `TrenchSectionTests`. About 20 developer-days for the three.

## Order and size
SIM-A and Phase 1 first (no dependencies), then the death batches, scatter and trench, SIM-B/C with the targeting
UI and effects, the meta scenes and screens; SIM-D, the upgrades seam and the staging pool swap wait for
`lane/show/units-meta`. About 60 developer-days on the combat and meta halves before the environment half.

## Status (2026-09-26)
- **SIM-A** (replay v5) and **SIM-B** (replay v6: `AbilityArgs`, HE patterns, creeping barrage, creeping gas, smoke
  screen, strafe run; `docs/02` change log) are on `lane/sim/overhaul` and merged into `lane/show/overhaul`.
- **Phase 1** core: `AssetScaleTable`, the enforcement at emit, the audit writer and `Tools/looks.py` applied. The
  audit report and the before/after captures wait for an editor.
- **Phase 4** on the show branch: the death ladder, density, tumble, heap, the char look and the smoulder
  (`AnimationController.Death.cs`, `VATRenderer.Fallen.cs`, `CombatFx.Deaths.cs`, `VAT_URP`), `DeathVarietyTests`.
  Not yet: the six new clips and their bake (D1: an editor session), so `DeathBurning`, `DeathGas`, `DeathCrushed`,
  `DeathStagger`, `DeathWalking2` and `DeathBackHeadshot` have stand-ins in the ladder.
- **Phase 2** on the show branch: the composer split into six partials (no behaviour change) and the scatter engine
  (`ScatterField.cs`, `ScatterLayers.cs`, `BattlefieldComposer.Scatter.cs`, the kit's `grassMicro`, `poppiesMicro`
  and `lantern`), `ScatterRulesTests`; `docs/13` "Rule-based scatter". Not yet: the per-instance tint, the lantern
  coupling to NightLights, the captures and the draw/vertex counts.
- **Phase 3** on the show branch: the lining's damaged twins, `TrenchSection.cs`, the section state machine in
  `PropDestruction` and `PropWear` (shared key, `Replace`, `AddInstance`, `Break`, `LiningLife`), `ZoomShare`,
  `TrenchSectionTests`; `docs/16` "The lining breaks in two steps". Not yet: `FrameBudget` coverage of the props'
  draws and the barrage bench.
- **Phase 5 show side (A1/A2)** on the show branch: `AbilityAim` (point / press-drag-release, Tab patterns, Shift
  snap) driving `TestPanel`, six support cards with keys 9 0 C M V B and text (`HudText.Support`), the readout's
  corridor count, `CombatFx.Abilities.cs` (aim shapes, the strafe's flyover through `SceneHooks.Biplane` and its
  tracers, the beam's charge, column and scorch, the smoke screen's cards), `AbilityAimTests`. Not yet: an
  `AbilityTargeting` controller in `TW.UI` (the aim stays with `TestPanel`, which owns the ground pick), portraits
  of their own for the four new cards (they borrow the baked two), a `Beam` shader and scorch marks (a new
  `GroundMark` shape), the sapper's buttons.
- **Nothing has been through the EditMode/PlayMode gate:** every commit was compiled offline with Roslyn; the
  machine had under 1 GB of RAM free. Run `gate.ps1` in both worktrees before merging.
- **Phase 6 data layer** on the show branch: `CampaignGraph` (five country nodes, 17 missions in order with their
  grounds and seeds, prerequisites, gold 25 / 40 / 75, difficulty presets), `FactionBuildings` (seven Iron and
  eight Brass buildings on the Military and Houses sets, stages 40 / 80 / 150 capping tiers at 2n, unit tracks
  15..100, global rows 30 / 60 / 120, ability unlocks), `CampaignProfile` + `ProfileStore`, `CampaignSession`,
  `MetaViews` (the seam the screens and the 3D views meet at), three test classes. The tables are code, not
  assets: nothing to build in an editor. Not yet: the Meta assembly (diorama, map, camera), the three screens,
  the debrief's gold, the sim's upgrade seam (B1 waits for units-meta).
- **Next:** the Meta views and the campaign screens, then B1 and SIM-D once units-meta lands.
